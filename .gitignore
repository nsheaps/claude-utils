# Dependencies
node_modules/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Build outputs
dist/
*.tar.gz

# Compiled CLI binaries (built with `bun build --compile`, not committed —
# produced by `mise run build` locally/CI and shipped via release assets)
bin/agent-hook
bin/agent-plugin

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Test artifacts
test/tmp/

# mise local config
.mise.local.toml

# NX monorepo stuff
.nx/
