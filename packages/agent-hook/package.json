{
  "name": "agent-hook",
  "version": "0.1.0",
  "private": true,
  "description": "Emits the JSON a Claude Code hook answers with, and flattens the hook payload into shell variables.",
  "claudeUtilsCli": "agent-hook",
  "scripts": {
    "build": "bun ../../scripts/build-cli.mjs",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint",
    "test": "node --test test/*.test.mjs"
  }
}
